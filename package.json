{
  "name": "pi-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm -w server run dev\" \"npm -w web run dev\"",
    "dev:server": "npm -w server run dev",
    "dev:web": "npm -w web run dev",
    "install:all": "npm -w server install && npm -w web install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}

